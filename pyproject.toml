[project]
name = "lablistmaker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.0",
]

[dependency-groups]
dev = [
    "openpyxl>=3.1.5",
]
